## Agent skills

### Issue tracker

Issues and PRDs are tracked in GitHub Issues for `ViniciusMoraesSilva/arquitetura_dados` using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

This repo uses the default canonical triage label vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

This repo uses a single-context domain documentation layout. See `docs/agents/domain.md`.
